<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>wbif.sjx</groupId>
    <artifactId>ModularImageAnalysis</artifactId>
    <version>0.2.4</version>
    <packaging>jar</packaging>
    <name>Modular_Image_Analysis</name>
    <url>http://maven.apache.org</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <myscope>compile</myscope>
        <buildDir>${project.build.directory}/${project.name}-${project.version}/plugins/WBIF_Bristol</buildDir>
        <dependenciesDir>${project.build.directory}/${project.name}-${project.version}/jars/WBIF_Bristol</dependenciesDir>
        <addClasspathCnd>false</addClasspathCnd>
        <classpathPref></classpathPref>
        <exclScope></exclScope>
        <exclArtifactsID></exclArtifactsID>
    </properties>

    <profiles>
        <profile>
            <id>build-plugin</id>
            <properties>
                <buildDir>${project.build.directory}/${project.name}-${project.version}/plugins/WBIF_Bristol</buildDir>
                <dependenciesDir>${project.build.directory}/${project.name}-${project.version}/jars/WBIF_Bristol</dependenciesDir>
                <addClasspathCnd>false</addClasspathCnd>
                <classpathPref></classpathPref>
                <exclScope>provided</exclScope>
                <exclArtifactsID>commons-codec,commons-collections,commons-io,hamcrest-core,guava,jama,tools,jdk.tools,javassist,junit,MorphoLibJ_</exclArtifactsID>
            </properties>
        </profile>
        <profile>
            <id>build-cli</id>
            <properties>
                <buildDir>${project.build.directory}/MIA/</buildDir>
                <dependenciesDir>${project.build.directory}/MIA/lib</dependenciesDir>
                <addClasspathCnd>true</addClasspathCnd>
                <classpathPref>lib</classpathPref>
                <exclScope></exclScope>
                <exclArtifactsID></exclArtifactsID>
            </properties>
        </profile>
    </profiles>

    <repositories>
        <repository>
            <id>imagej.public</id>
            <url>http://maven.imagej.net/content/groups/public</url>
        </repository>

        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>

        <repository>
            <id>ome-web-prod.lifesci.dundee.ac.uk</id>
            <name>ome-web-prod.lifesci.dundee.ac.uk-releases</name>
            <url>http://artifacts.openmicroscopy.org/artifactory/ome.releases</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.12</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>3.16</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
            <scope>${myscope}</scope>
        </dependency>

        <dependency>
            <groupId>ome</groupId>
            <artifactId>bio-formats_plugins</artifactId>
            <version>5.5.3</version>
            <scope>${myscope}</scope>
        </dependency>

        <dependency>
            <groupId>net.imagej</groupId>
            <artifactId>ij</artifactId>
            <version>1.51h</version>
            <scope>${myscope}</scope>
        </dependency>
        <dependency>
            <groupId>sc.fiji</groupId>
            <artifactId>Fiji_Plugins</artifactId>
            <version>3.1.1</version>
            <scope>${myscope}</scope>
        </dependency>
        <dependency>
            <groupId>sc.fiji</groupId>
            <artifactId>Auto_Threshold</artifactId>
            <version>1.16.4</version>
            <scope>${myscope}</scope>
        </dependency>
        <dependency>
            <groupId>sc.fiji</groupId>
            <artifactId>TrackMate_</artifactId>
            <version>3.5.1</version>
            <scope>${myscope}</scope>
        </dependency>
        <dependency>
            <groupId>sc.fiji</groupId>
            <artifactId>CorrectBleach_</artifactId>
            <version>2.0.2</version>
            <scope>${myscope}</scope>
        </dependency>

        <dependency>
            <groupId>fr.inra.ijpb</groupId>
            <artifactId>MorphoLibJ_</artifactId>
            <version>1.3.2</version>
            <scope>${myscope}</scope>
        </dependency>

        <dependency>
            <groupId>de.biomedical-imaging.imagej</groupId>
            <artifactId>ij_ridge_detect</artifactId>
            <version>1.4.0</version>
            <scope>${myscope}</scope>
        </dependency>

        <dependency>
            <groupId>org.reflections</groupId>
            <artifactId>reflections</artifactId>
            <version>0.9.11</version>
        </dependency>

        <dependency>
            <groupId>org.apache.hbase</groupId>
            <artifactId>hbase-server</artifactId>
            <version>1.3.0</version>
        </dependency>

        <dependency>
            <groupId>org.ehcache</groupId>
            <artifactId>ehcache</artifactId>
            <version>3.4.0</version>
        </dependency>

        <dependency>
            <groupId>wbif.sjx</groupId>
            <artifactId>Common</artifactId>
            <version>1.3.1</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.6</version>
                <configuration>
                    <outputDirectory>${buildDir}</outputDirectory>
                    <finalName>Modular_Image_Analysis-${project.version}</finalName>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                            <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                            <addClasspath>${addClasspathCnd}</addClasspath>
                            <classpathPrefix>${classpathPref}</classpathPrefix>
                            <mainClass>wbif.sjx.ModularImageAnalysis.ModularImageAnalysisPlugin</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.0.0</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <excludeScope>${exclScope}</excludeScope>
                            <excludeArtifactIds>${exclArtifactsID}</excludeArtifactIds>

                            <outputDirectory>${dependenciesDir}</outputDirectory>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>