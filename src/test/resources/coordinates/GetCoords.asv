clear

pnameIn = 'C:\Users\steph\Documents\Java Projects\ModularImageAnalysis\src\test\resources\images\';
fnameIn = 'LabelledObjects4D_8bit.tif';

pnameOut = 'C:\Users\sc13967\Documents\Java_Projects\ModularImageAnalysis\src\test\resources\coordinates\';
fnameOut = 'ExpectedObjects4D.csv';

nZ = 12;
nT = 4;

for i = 1:numel(imfinfo([pnameIn,fnameIn]))
   im(:,:,i) = imread([pnameIn,fnameIn],(i-1)*nZ+1);    
end

[x,y,z] = ind2sub(size(im),find(im==0));
coords = [];
for i=1:numel(x)
   newCoord = [im(x(i),y(i),z(i)),im(x(i),y(i),z(i)),y(i)-1,x(i)-1,0,z(i)-1,t];
   coords = [coords;newCoord];
    
end

csvwrite([pnameOut,fnameOut],coords);